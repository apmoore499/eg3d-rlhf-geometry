# @package _global_

defaults:
  - override /hydra/sweeper: optuna

# choose metric which will be optimized by Optuna
# make sure this is the correct name of some metric logged in lightning module!
optimized_metric: "val_epoch/total_loss"

data:
  dset_dict:
    proportion_of_data_to_use: 0.3


trainer:
  max_epochs: 6


test: False
sync: False
optuna_tuning: True
# here we define Optuna hyperparameter search
# it optimizes for value returned from function with @hydra.main decorator
hydra:
  sweeper:
    _target_: hydra_plugins.hydra_optuna_sweeper.optuna_sweeper.OptunaSweeper
    # 'minimize' or 'maximize' the objective
    #direction: minimize
    direction: 
    - maximize
    - minimize
    sampler:
      seed: 123

    storage: null
    study_name: null
    n_trials: 25
    n_jobs: 1
    params:
      model.optimizer.lr: interval(1e-6,1e-3)
      model.optimizer.weight_decay: interval(0.0, 1e-7)
      batch_size_all: 8
      model.act_type: cos #choice(softplus,cos) #,cummax_mean,cummax_std)

#val_epoch/total_loss

#"cummax_mean": "cumsum_norm" "cummax_std"



