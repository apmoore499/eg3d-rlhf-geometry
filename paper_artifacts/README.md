This top-level directory holds small tracked paper-supporting result artifacts.

It exists to keep summary outputs separate from source-code directories.

Rules:

- keep only small text-based or lightweight paper-supporting artifacts here
- do not turn this into a dump of full run directories
- do not put heavyweight checkpoints here

Current contents:

- `reward_embedding_analysis/`
  Summary reward-analysis artifacts retained for the paper-backed public
  surface.
