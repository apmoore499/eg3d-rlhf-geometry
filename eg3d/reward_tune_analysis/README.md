Project-local analysis scripts for EG3D reward tuning live here.

Contents:

- analysis and comparison scripts for snapshot spectra, sigma fields, and
  reward-conditioned structure
- `scripts/` for local operational entrypoints used to launch reward-tuning runs
- `legacy/` for older one-off tools that are kept only for reference
