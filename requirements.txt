# Curated dependencies for EG3D RLHF Geometry, pinned to the versions verified to work
# together with torch 2.0.1 / CUDA 11.8 (Python 3.9). Install into the env from the
# README "Setup" section. Packages needing a special install (compiled or wheel-matched)
# are listed, commented, at the bottom -- install those per the README.

# --- pinned base (installed by conda; pinned so pip will not upgrade them) ---
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2
numpy==1.26.4

# --- core: reward-model training + EG3D fine-tuning ---
pandas==2.3.3
scipy==1.13.1
scikit-learn==1.6.1
scikit-image==0.24.0
matplotlib==3.9.4
Pillow==11.3.0
opencv-python-headless==4.11.0.86
tqdm==4.67.1
PyYAML==6.0.2
hydra-core==1.3.2
omegaconf==2.3.0
pytorch-lightning==2.3.3
wandb==0.23.1
click==8.1.8
rich==14.2.0
einops==0.8.1
trimesh==3.23.5
mrcfile==1.5.4
monai==1.4.0
lpips==0.1.4
kornia==0.8.2

# --- GUI / rendering (optional: the EG3D visualizer and mesh preview) ---
imgui==1.4.1
glfw==2.10.0
PyOpenGL==3.1.0
pyrender==0.1.45
pyvista==0.46.4

# --- analysis / figures (optional: paper post-hoc analyses) ---
seaborn==0.13.2
umap-learn==0.5.12
statsmodels==0.14.6
plotly==6.5.0
pandas-ods-reader==1.0.2
pyexcel==0.7.4

# --- data generation / preprocessing (optional) ---
facenet-pytorch==2.5.3
mtcnn==0.1.1
mediapipe==0.10.21
torchlm==0.1.6.10
gdown==5.2.2
DracoPy==2.0.0
ml_collections==0.1.1

# --- dev / test (optional) ---
pytest==8.4.2
ipython==8.18.1
mlflow==3.1.4

# --- installed separately (see README "Setup"), NOT via `pip install -r` ---
# torch, torchvision : conda (pinned above as a guard; pip must not change them)  [required]
# pytorch-3dunet     : conda-forge; the sigma-field backbone (ResidualUNetSE3D)   [required]
# dlib               : conda-forge; loads the bundled 5-point landmark model       [required]
# pytorch3d          : prebuilt wheel (README)                                     [required]
# torch-scatter      : ==2.1.1 -f https://data.pyg.org/whl/torch-2.0.1+cu118.html  [required]
# torch-geometric    : pip install torch-geometric (pure python)                  [required]
# open3d             : pip; optional, for point-cloud visualisation
# pyspng             : optional PNG-decode speed-up (code falls back to PIL); finicky to build
# pygem (PyGeM)      : pip from https://github.com/mathLab/PyGeM; optional analysis
